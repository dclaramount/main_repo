version: '3.8'

services:
  app:
    build:
      context: ..
      dockerfile: ./docker/Build.DockerFile
    ports:
      - "3000:3000"
    restart: always
    environment:
      NODE_ENV: production